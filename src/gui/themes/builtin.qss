QPushButton, QToolButton, QComboBox {
    background: palette(button);
    border: 1px solid palette(mid);
    border-radius: 6px;
    padding: 5px 10px;
}
QToolButton {
    padding: 5px;
}
QToolButton[popupMode="1"] {
    padding-right: 20px;
}
QToolButton::menu-button {
    background: palette(button);
    border: none;
    border-left: 1px solid palette(mid);
    width: 16px;
}
QToolButton::menu-button:hover {
    background: palette(midlight);
}
QToolBar, QMenuBar {
    background: palette(window);
    border: none;
}
QToolBar {
    spacing: 2px;
    padding: 3px 8px;
}
QToolBar QToolButton {
    background: transparent;
    border-color: transparent;
    padding: 3px;
}
QToolBar::separator {
    width: 4px;
}
QToolBar QToolButton:hover, QToolBar QToolButton:checked {
    background: palette(midlight);
}
QPushButton:hover:enabled, QToolButton:hover:enabled, QComboBox:hover:enabled {
    background: palette(midlight);
    border-color: palette(light);
}
QPushButton:pressed, QPushButton:checked, QToolButton:pressed, QToolButton:checked {
    background: palette(highlight);
    border-color: palette(mid);
}
QPushButton:default, QPushButton:focus, QToolButton:focus, QComboBox:focus {
    border-color: palette(link);
}
QPushButton:checked:disabled, QToolButton:checked:disabled {
    background: palette(midlight);
}
QLineEdit, QAbstractSpinBox, QTextEdit, QPlainTextEdit {
    background: palette(base);
    border: 1px solid palette(mid);
    border-radius: 6px;
    padding: 4px 7px;
}
QLineEdit:focus, QAbstractSpinBox:focus, QTextEdit:focus, QPlainTextEdit:focus {
    border-color: palette(link);
}
QComboBox {
    padding-right: 24px;
}
QComboBox QAbstractItemView {
    background: palette(base);
    selection-background-color: palette(highlight);
    selection-color: palette(highlighted-text);
    border: 1px solid palette(mid);
    outline: none;
}
QComboBox::drop-down {
    border: none;
    width: 22px;
}
QComboBox::down-arrow, QAbstractSpinBox::down-arrow, QToolButton::menu-arrow, QScrollBar::down-arrow {
    image: url(:/themes/chevron-down.svg);
    width: 10px;
    height: 6px;
}
QAbstractSpinBox::up-arrow, QScrollBar::up-arrow {
    image: url(:/themes/chevron-up.svg);
    width: 10px;
    height: 6px;
}
QAbstractSpinBox::up-button, QAbstractSpinBox::down-button {
    background: palette(button);
    border: none;
    width: 18px;
}
QAbstractSpinBox::up-button:hover, QAbstractSpinBox::down-button:hover {
    background: palette(midlight);
}
QCheckBox::indicator, QGroupBox::indicator, QAbstractItemView::indicator, QMenu::indicator:non-exclusive {
    background: palette(base);
    border: 1px solid palette(mid);
    border-radius: 4px;
    width: 15px;
    height: 15px;
}
QCheckBox, QRadioButton {
    spacing: 7px;
}
QCheckBox::indicator:checked, QGroupBox::indicator:checked,
QAbstractItemView::indicator:checked, QMenu::indicator:non-exclusive:checked {
    background: #009df7;
    border-color: #009df7;
    image: url(:/themes/check.svg);
}
QCheckBox::indicator:indeterminate, QAbstractItemView::indicator:indeterminate {
    background: #009df7;
    border-color: #009df7;
    image: url(:/themes/dash.svg);
}
QRadioButton::indicator {
    background: palette(base);
    border: 1px solid palette(mid);
    border-radius: 8px;
    width: 15px;
    height: 15px;
}
QRadioButton::indicator:checked {
    background: #009df7;
    border-color: #009df7;
    image: url(:/themes/radio.svg);
}
QCheckBox::indicator:checked:disabled, QCheckBox::indicator:indeterminate:disabled,
QGroupBox::indicator:checked:disabled, QAbstractItemView::indicator:checked:disabled,
QAbstractItemView::indicator:indeterminate:disabled, QMenu::indicator:non-exclusive:checked:disabled {
    background: palette(midlight);
}
QCheckBox::indicator:hover:enabled, QGroupBox::indicator:hover:enabled {
    border-color: palette(link);
}
QRadioButton::indicator:hover:enabled {
    border-color: palette(link);
}
QGroupBox {
    border: none;
    margin-top: 17px;
    padding: 0;
}
QGroupBox::title {
    subcontrol-origin: margin;
    subcontrol-position: top left;
    padding: 0;
}
QMenu {
    background: palette(window);
    border: 1px solid palette(mid);
}
QMenuBar::item {
    background: transparent;
    padding: 4px 8px;
}
QMenuBar::item:selected, QMenuBar::item:pressed {
    background: palette(midlight);
}
QMenu::item {
    padding: 6px 28px 6px 24px;
}
QMenu::item:selected {
    background: palette(highlight);
    color: palette(highlighted-text);
}
QMenu::separator {
    background: palette(mid);
    height: 1px;
    margin: 3px 6px;
}
QHeaderView::section {
    background: palette(window);
    border: none;
    border-bottom: 1px solid palette(mid);
    padding: 6px 8px;
}
QAbstractItemView {
    background: palette(base);
    alternate-background-color: palette(alternate-base);
    selection-background-color: palette(highlight);
    selection-color: palette(highlighted-text);
    outline: none;
}
QAbstractItemView::item:hover {
    background: palette(midlight);
}
QListWidget#tabSelection {
    background: palette(window);
    border: none;
    outline: none;
}
QListWidget#tabSelection::item {
    border-radius: 6px;
    margin: 2px 5px;
    padding: 7px 9px;
}
QListWidget#tabSelection::item:selected {
    background: palette(highlight);
    color: palette(highlighted-text);
}
QTabBar::tab {
    background: palette(window);
    border: none;
    border-bottom: 2px solid transparent;
    padding: 6px 12px;
}
QTabBar::tab:hover {
    background: palette(midlight);
}
QTabBar::tab:selected {
    background: palette(button);
    border-bottom-color: palette(link);
}
QTabWidget::pane {
    border: none;
}
QProgressBar {
    background: palette(base);
    border: none;
    border-radius: 4px;
    min-height: 7px;
    text-align: center;
}
QProgressBar::chunk {
    background: #009df7;
}
QScrollBar {
    background: palette(window);
}
QScrollBar:horizontal {
    height: 12px;
    margin: 0 12px;
}
QScrollBar:vertical {
    width: 12px;
    margin: 12px 0;
}
QScrollBar::handle {
    background: palette(mid);
    border: none;
    border-radius: 5px;
}
QScrollBar::handle:horizontal {
    min-width: 20px;
}
QScrollBar::handle:vertical {
    min-height: 20px;
}
QScrollBar::handle:hover {
    background: palette(light);
}
QScrollBar::add-line, QScrollBar::sub-line {
    background: palette(window);
    border: none;
    width: 12px;
    height: 12px;
    subcontrol-origin: margin;
}
QScrollBar::sub-line:horizontal {
    subcontrol-position: left;
}
QScrollBar::add-line:horizontal {
    subcontrol-position: right;
}
QScrollBar::sub-line:vertical {
    subcontrol-position: top;
}
QScrollBar::add-line:vertical {
    subcontrol-position: bottom;
}
QScrollBar::left-arrow {
    image: url(:/themes/chevron-left.svg);
    width: 6px;
    height: 10px;
}
QScrollBar::right-arrow {
    image: url(:/themes/chevron-right.svg);
    width: 6px;
    height: 10px;
}
QScrollBar::add-page, QScrollBar::sub-page {
    background: palette(base);
}
QSlider::groove:horizontal {
    background: palette(mid);
    height: 4px;
}
QSlider::sub-page:horizontal {
    background: #009df7;
}
QSlider::handle:horizontal {
    background: palette(light);
    border-radius: 5px;
    width: 10px;
    margin: -4px 0;
}
QSlider::handle:horizontal:hover:enabled {
    background: palette(link);
}
QSlider::handle:horizontal:disabled {
    background: palette(mid);
}
